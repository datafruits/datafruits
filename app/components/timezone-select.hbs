<PowerSelect
  class="user-settings-input h-9"
  @searchEnabled={{true}}
  @renderInPlace={{true}}
  @options={{this.availableTimeZones}}
  @selected={{@selectedTimezone}}
  @onChange={{@onChange}}
  as |timeZone|>
      {{timeZone}}
</PowerSelect>

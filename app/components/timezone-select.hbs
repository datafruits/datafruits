<PowerSelect
  class="user-settings-input timezone-input h-9"
  @searchEnabled={{true}}
  @renderInPlace={{true}}
  @options={{this.availableTimeZones}}
  @selected={{@selectedTimezone}}
  @onChange={{@onChange}}
  as |timeZone|>
      {{timeZone}}
</PowerSelect>

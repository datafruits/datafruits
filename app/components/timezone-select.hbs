<PowerSelect
  @searchEnabled={{true}}
  @renderInPlace={{true}}
  @options={{this.availableTimeZones}}
  @selected={{@selectedTimezone}}
  @onChange={{@onChange}}
  as |timeZone|>
      {{timeZone}}
</PowerSelect>

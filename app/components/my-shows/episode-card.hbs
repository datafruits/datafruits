<div class="mb-2">
  <LinkTo
    @route="home.shows.episode"
    @models={{array @episode.showSeries.slug @episode.slug}}
    class="text-2xl">
    {{@episode.title}}
  </LinkTo>
  <img src={{@episode.imageUrl}} />
  <LinkTo
    @route="home.user.my-shows.episode"
    @models={{array @episode.showSeries.slug @episode.slug}}
    class="text-2xl">
    {{t "profile.my-shows.edit"}}
  </LinkTo>
</div>
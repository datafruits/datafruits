<PowerSelectMultiple
  @options={{this.groupedFilters}}
  @selected={{this.filter}}
  @placeholder="Select some filters..."
  @onChange={{fn (mut this.filter)}} as |filter|>
>
  <img class="w-8" src={{filter.img}}/>
  {{filter.name}}
</PowerSelectMultiple>
<div class="main-content">
  <div class="row">
    {{#each model as |dj|}}
      <div class="col-md-4">
        <h2>{{#link-to 'home.dj' dj.username}}{{dj.username}}{{/link-to}}</h2>
        <img src={{model.imageThumbUrl}} alt={{model.username}}>
      </div>
    {{/each}}
  </div>
</div>

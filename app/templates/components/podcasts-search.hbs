{{input class="search-tracks" autofocus="autofocus" value=filterText escape-press='clearSearch' placeholder="Search podcasts"}}
{{#power-select-multiple
    placeholder="Search by tag..."
    options=labels
    selected=selectedLabels
    searchField="name"
    renderInPlace=true
    onchange=(action (mut selectedLabels))
    as |label|
}}
  {{label.name}}
{{/power-select-multiple}}
{{#if isSearching}}
  {{#each filteredResults as |track|}}
    {{podcast-track cdn_url=track.cdn_url title=track.title labels=track.labels}}
  {{/each}}
{{else}}
  {{#each podcasts as |track|}}
    {{podcast-track cdn_url=track.cdn_url title=track.title labels=track.labels}}
  {{/each}}
{{/if}}
